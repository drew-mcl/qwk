MESSAGE="Hello, World!"

greeting:
	echo $(MESSAGE)

say:
	echo "$1"

build-windows:
	GOOS=windows GOARCH=amd64 go build ./cmd/qwk.go 

build:
	go build cmd/qwk.go	

commit:
	git add .
	git commit -m "$@"

push:
	git push

git: commit push:
